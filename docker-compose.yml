services:
  postgres:
    image: postgres:16-alpine
    container_name: sports-articles-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-sports}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-sports}
      POSTGRES_DB: ${POSTGRES_DB:-sports_articles}
    ports:
      - '${POSTGRES_PORT:-5432}:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test:
        ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-sports} -d ${POSTGRES_DB:-sports_articles}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  pgdata:
